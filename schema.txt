CREATE TABLE logs (entry INT AUTO_INCREMENT PRIMARY KEY,
    pilot VARCHAR(128) NOT NULL,
    date DATE NOT NULL,
    end_tach DECIMAL(6,2) NOT NULL,
    end_hobbs DECIMAL(6,2) NOT NULL,
    start_oil DECIMAL(2,2),
    oil_added DECIMAL(2,2),
    note VARCHAR(255)
    );
